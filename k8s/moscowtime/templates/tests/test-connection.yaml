apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moscowtime.fullname" . }}-test-connection"
  labels:
    {{- include "moscowtime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moscowtime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
